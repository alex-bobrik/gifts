{% extends 'base.html.twig' %}

{% block title %}Hello MapOptionController!{% endblock %}

{% block body %}
    <div class="container">
        <table class="table">
            <thead>
                <th scope="col">Map Option</th>
                <th scope="col">Action</th>
            </thead>
            <tbody>
            {% for mapOption in mapOptions %}
            <tr>
                <td>
                    {{ mapOption.name }}
                </td>
                <td>
                    <a href="{{ path('admin_mapOption_edit', {'id': mapOption.id}) }}" class="btn btn-info">Edit</a>
                    <a href="{{ path('admin_mapOption_delete', {'id': mapOption.id}) }}" class="btn btn-danger">Delete</a>
                </td>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
